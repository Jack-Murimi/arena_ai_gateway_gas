name: arena_ai_gateway_gas
description: "Gateway Gas Enterprises — POS & ERP system for gas cylinder sales, deposits, credit and stock."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  provider: ^6.1.2
  intl: ^0.20.2
  uuid: ^4.5.1
  share_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
